{
    "scripts": {
        "dev-app": "npx concurrently --kill-others -c 'blue,cyan' -n 'devserve,electron' 'cd game && npm run dev' 'cd app && npm run dev'",
        "dev-game": "cd game && npm run dev",
        "build-app": "cd app && npm run build",
        "build-game": "cd game && npm run build"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}
