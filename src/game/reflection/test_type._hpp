#pragma once

// REVOKED FROM COMPILATION
// Refer to object.hpp for more information on why this file is not being used in
// the project.

#include "property.hpp"
#include "object.hpp"

namespace game {
    class test_type : public object {
    public:
        test_type(const std::string_view name) : object(name) {
            define<basic_property<int>>("diamonds");
            define<basic_property<float>>("cash");
            //define<basic_property<std::string>>("string"); // shouldnt work here
        }

        void v() {
            auto& diamonds = get<basic_property<int>>("diamonds");
            auto& cash = get<basic_property<float>>("cash");
            //auto& string = get<basic_property<std::string>>("string");

            diamonds = 5;
            cash = 5.0f;
            //string = "hello";
        }
    };
}